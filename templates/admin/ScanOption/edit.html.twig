{% extends 'admin/base.html.twig' %}

{% block body %}

    <h1>Editer une option de scan</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="">Valider</button>

    {{ form_end(form) }}

{% endblock%}